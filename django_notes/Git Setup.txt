* git init
* git status 
* git add .
* git status 
* git commit -m "django project setup"
* git branch (# main xa vane kei vayeana master xa vane main ma lane , code:)
* git branch -M main
* git remote add (# teha github ma deko hunxa yo remote link tyo copy hanne)
* git push -u origin main





***
first ma git setup garda:-
            aafno laptop ma first time git setup garda:

                (
                    git config --global user.name "your github user name"(git hub account create garda rakheko username )
                    git config --global user.email "your registered email"
                )
                yo ekchoti matra setup gare hunxa euta laptop ma


                ani vayo ki nai vanera check garna chai : 
                
            ***